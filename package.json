{
  "name": "ssh-chain",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run --watch index.ts",
    "typecheck": "tsc --noEmit",
    "build": "bun build --compile --minify --sourcemap ./index.ts --outfile dist/ssh-chain"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "ink": "^6.6.0",
    "proxy-chain": "^2.7.1",
    "react": "^19.2.3",
    "zod": "^4.3.6"
  }
}
